#include<stdio.h>
#include<stdlib.h>
#include<time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char num;
char nl;
nl = 012;
for (num = 48 ; num < 58 ; num++)
{
putchar(num);
}
putchar(nl);
return (0);
}
