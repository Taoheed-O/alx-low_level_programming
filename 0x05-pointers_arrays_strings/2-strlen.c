#include "main.h"
#include<string.h>
/**
* _strlen - returns the length of a string
* @s: the char parameter
* Return: the length of the string s
*/
int _strlen(char *s)
{
int length;
length = strlen(s);
return (length);
}
