#include "main.h"
/**
* swap_int - swapping the values of the two parameters
* @a: the first integer
* @b: the second integer
* Return: Always 0 (Success)
*/
void swap_int(int *a, int *b)
{
*b = *a;
}
